import React from 'react';
import { PlaceholderScreen } from '../../components/PlaceholderScreen';

// Design ref: Screen.dc.html — is.forgot (confirmation replaces the form in place)
export function ForgotPasswordScreen() {
  return <PlaceholderScreen title="Reset password" note="Confirmation replaces the form in place, not a new screen" />;
}
