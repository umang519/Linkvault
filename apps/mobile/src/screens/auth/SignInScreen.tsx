import React from 'react';
import { PlaceholderScreen } from '../../components/PlaceholderScreen';

// Design ref: Screen.dc.html — is.signin (has an inline error state)
export function SignInScreen() {
  return <PlaceholderScreen title="Sign in" note="Email + password, inline error state, Forgot / Create account links" />;
}
